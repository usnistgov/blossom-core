package contract;
/*

import org.hyperledger.fabric.contract.Context;
import org.hyperledger.fabric.contract.annotation.Contract;
import org.hyperledger.fabric.contract.annotation.Info;
import org.hyperledger.fabric.contract.annotation.Transaction;

@Contract(
        name = "bootstrap",
        info = @Info(
                title = "Blossom asset chaincode bootstrap contract",
                description = "Chaincode functions to bootstrap the Blossom asset chaincode",
                version = "0.0.1"
        )
)
public class BootstrapContract {

        // load policy
        // i think all data will be stored in the IPDC of the SAMS member so nothing is on public ledger
        // can only bootstrap is the SAMS member is authorized
        @Transaction
        public void bootstrap(Context ctx) {


        }


}
*/
